<br />
<h2>All Users</h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <%= link_to 'Delete', admin_user_path(user), method: :delete, data: { confirm: 'Do you really want to delete this？' } %>
    </li>
  <% end %>
</ul>